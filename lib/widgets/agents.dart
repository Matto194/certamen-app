import 'package:flutter/material.dart';

class Agents extends StatelessWidget {
  final String nombre;
  final String foto;

  Agents({required this.nombre, required this.foto});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
